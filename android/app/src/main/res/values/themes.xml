<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Stylit" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>
    <style name="Theme.Stylit" parent="Base.Theme.Stylit">
        <item name="android:windowBackground">@color/darkest_gray</item>

        <item name="android:colorAccent">@color/vivid_red</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/vivid_red</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/darker_gray</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@android:color/transparent</item>

        <item name="android:navigationBarColor">@color/darkest_gray</item>

    </style>
    <style name="Theme.Stylit.Navigation" parent="Widget.MaterialComponents.BottomNavigationView.PrimarySurface">
        <item name="android:background">@color/darkest_gray</item>
    </style>
</resources>